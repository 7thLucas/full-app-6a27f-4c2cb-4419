apiVersion: apps/v1
kind: Deployment
metadata:
  name: g0fpf2x9-4419-staging
  namespace: qb-prototypes
spec:
  replicas: 1
  selector:
    matchLabels:
      app: g0fpf2x9-4419-staging
  template:
    metadata:
      labels:
        app: g0fpf2x9-4419-staging
    spec:
      imagePullSecrets:
      - name: qtn-dockerhub
      containers:
      - name: web
        image: quantumteknologi/g0fpf2x9-4419-staging
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: "500m"
            memory: "512Mi"
          limits:
            cpu: "2"
            memory: "2Gi"
        env:
        - name: WATCHPACK_POLLING
          value: "true"
